Single thresded application

function sayHiLater(){
    var greeting = 'Hi';
    setTimeout(function(){
        console.log(greeting);
    }, 3000);
}

sayHiLater();

function stack calls webapi and then webpai put the function in event queue which is then send to function stack when it is empty
